<link rel="stylesheet" href="css/blueimp-gallery.min.css">
<link rel="stylesheet" href="css/demo.css">
<% if (!album.isRoot){ %>
  <% for (var i=0; i<breadcrumb.length; i++) { %>
  <% if (i==breadcrumb.length-1){ %>
  <%= breadcrumb[i].name %>
  <% }else{ %>
  <a href="<%= breadcrumb[i].url %>"> <%= breadcrumb[i].name %> &raquo; </a>
  <% } %>

  <% } %>
<% } %>
  <% if (album.description){ %>
    <p><%- album.description %></p>
  <% } %>

  <% if (album.albums.length>0){ %>
  <h2>Albums</h2>
  <ul class="albums">
    <% album.albums.forEach(function(album){ %>
      <li class="thumb">
        <a href="<%= rootDir%>/<%= album.path%>">
          <div class="image"><img src="/<%= directory %>/<%= album.thumb %>&tn=1" alt="<%= album.prettyName %>"><br /></div>
          <span class="caption"><%= album.prettyName %></span>
        </a>
      </li>
    <% }) %>
  </ul>
<% } %>
<% if (album.photos.length>0){ %>
<div id="blueimp-gallery" class="blueimp-gallery">
    <div class="slides"></div>
    <h3 class="title"></h3>
    <a class="prev">‹</a>
    <a class="next">›</a>
    <a class="close">×</a>
    <a class="play-pause"></a>
    <ol class="indicator"></ol>
</div>
<div id="links">
    <% album.photos.forEach(function(photo){ %>
      <a class="item" href="/<%= directory %>/<%= photo.path %>" title="<%= photo.name%>">
        <img src="/thumbs<%= photo.path %>" alt="<%= photo.name%>">
      </a>
    <% }) %>
</div>
<% } %>
<script src="js/lib/blueimp-gallery.min.js"></script>
<script src="js/lib/masonry.pkgd.min.js"></script>
<script src="js/gallery.js"></script>
